# https://setuptools.pypa.io/en/latest/userguide/quickstart.html#basic-use
[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools"]

[project]
name = "setuptools-flat"
version = "0.0.1"
dependencies = []

[project.scripts]
marvin = "setuptools-flat.cli:main"
