# `pytest` with co-located test sources

Use `pytest` on a project in which test- and production sources are
side-by-side, in the same directory (that makes it easier to jump between
production and test code).
